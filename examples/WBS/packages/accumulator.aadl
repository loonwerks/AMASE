package accumulator
public
with Base_Types;

	system accumulator
		features
			-- Inputs
			circuit_pressure_value : in data port Base_Types::Integer;
			pressure_display : in data port Base_Types::Integer;
			-- Outputs
			reserve_out : out data port Base_Types::Integer;
	end accumulator;
	
	system implementation accumulator.inst
		
	end accumulator.inst;
end accumulator;